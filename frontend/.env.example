# Supabase project for tenorworth (create a NEW project; do not reuse SupremoAgent's).
# The anon key is safe to ship to the browser; RLS is the security boundary.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
